<Window
    x:Class="BreachQR.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:BreachQR"
    xmlns:vm="clr-namespace:BreachQR.ViewModels"
    xmlns:views="clr-namespace:BreachQR.Views"
    xmlns:math="http://hexinnovation.com/math"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="mwin"
    Title="MainWindow"
    Height="400"
    Width="400"
    d:Height="400"
    d:Width="400"
    AllowsTransparency="True"
    Background="Transparent"
    Deactivated="mwin_Deactivated"
    local:ViewModelLifecycleBehavior.ViewModel="{Binding CurrentViewModel}"
    ResizeMode="CanResizeWithGrip"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:SenderViewModel}">
            <views:Sender />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ReceiverViewModel}">
            <views:Receiver />
        </DataTemplate>
    </Window.Resources>
    <Grid Background="Transparent">
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>
